let a = 1;
let b = 2;

// Equality and inequality
print a == b;
print a != b;
print a == 1 + 0;
print b != 1 + 1;

// Comparison
print a < b;
print a > b;
print a <= b;
print a >= b;

// Comparisons mixed with arithmetic
print a + 1 == b;        // true: (a + 1) == b
print a * 2 == b;        // true: (a * 2) == b
print a + b > a;         // true: (a + b) > a
print a + b < a * b;     // false: (a + b) < (a * b)

// Nested precedence checks
print a + b == b + a;    // true
print a + b == a * b;    // false
print a + b * a == a + b * a; // true (check parens grouping)
print a - b < 0;       // true
print a - b < 0;         // true (no parens, should still work)


let bool = a - b < 0;

let false = 0 == 1;
let true = 1 == 1;
let finalTest = bool == true;
// bool test: " + finalTes