import "tcp";
import "Arrays";
import "async";

for {
    print "Enter a message to broadcast (or 'exit' to quit): ";
    let message = readLine();
    if message == "exit" {
        break;
    }
    print "Sending: " + message;
}